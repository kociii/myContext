# <version> 技术开发文档

> 本文档聚焦「如何实现」（技术架构、数据模型、接口设计）。
> 功能需求 → `prd.md`　　界面设计 → `design.md`　　时间排期 → `plan.md`

---

## 0. 项目现状审查

> **生成本文档前必须完成此步骤**：阅读现有代码，确保方案与实际架构对齐，不得使用模板占位符作为现状描述。

**审查清单（逐项确认后删除此节或标记为已完成）**：

- [ ] 已读取项目目录结构，了解整体组织方式
- [ ] 已确认实际技术栈（框架版本、核心依赖）
- [ ] 已读取核心入口文件和主要业务模块
- [ ] 已确认现有的数据模型（如有）
- [ ] 已了解现有的命名规范和代码组织模式
- [ ] 本次方案与现有架构无冲突

---

## 1. 架构概览

### 1.1 当前架构

> 基于实际代码审查填写，描述现有系统的真实架构（不使用占位符）

```
[根据实际代码审查结果填写架构图]
```

**当前技术栈**（从配置文件确认）：
- 前端：[实际框架和版本]
- 后端：[实际语言/框架和版本]
- 数据库/存储：[实际使用的存储方案]
- 关键依赖：[列出核心第三方依赖]

### 1.2 本次变更

描述本版本在现有架构基础上的变更点，以及变更原因。

---

## 2. 技术方案

### 2.1 方案对比

| 方案 | 优点 | 缺点 | 适用性 |
|------|------|------|--------|
| 方案 A | ... | ... | ... |
| 方案 B | ... | ... | ... |

**结论**：选择方案 X，原因：...

### 2.2 详细设计

#### 组件 A
- 职责：...
- 接口：...
- 数据流：...

#### 组件 B
- 职责：...
- 接口：...

---

## 3. 文件变更清单

> 列出本次开发**新增**和**改动**的文件，粒度到文件级别。

### 新增文件

| 文件路径 | 说明 |
|---------|------|
| `src/[模块]/[文件].ts` | 新增原因和职责 |

### 改动文件

| 文件路径 | 改动内容 |
|---------|---------|
| `src/[模块]/[文件].ts` | 新增/修改的函数或逻辑描述 |

### 删除文件

| 文件路径 | 原因 |
|---------|------|
| `src/[模块]/[旧文件].ts` | 被 XX 替代 / 功能废弃 |

> 无某类变更时填「无」，不可省略该小节。

---

## 4. 数据模型

如有数据库变更，描述新的/修改的数据结构。

```sql
-- 示例：新增表结构
CREATE TABLE ... (
  ...
);
```

> 完整 DDL 见 `sql/DDL.sql`，初始化数据见 `sql/DML_init.sql`

---

## 5. 接口影响清单

> 后端项目必含板块，记录当前版本对所有接口的影响

### 5.1 新增接口

| 接口 | 说明 | 影响点 |
|------|------|--------|
| `POST /xxx/add` | 新增功能说明 | 需新建 Controller/Service/Mapper |

### 5.2 改动接口

> 必须列出字段级别的调整说明

| 接口 | 改动内容 | 影响点 |
|------|----------|--------|
| `POST /xxx/query` | 响应新增 `fieldA`（String）、`fieldB`（Long）字段 | RespDTO 新增 2 个字段，Service 层增加聚合查询 |
| `POST /xxx/update` | 入参新增 `fieldC`（Integer）字段 | ReqDTO 新增 1 个字段，Service 层增加校验逻辑 |

### 5.3 删除接口

| 接口 | 说明 | 影响点 |
|------|------|--------|
| `POST /xxx/old` | 废弃旧接口说明 | 前端需迁移到新接口 |

---

## 6. 测试策略

- 单元测试覆盖
- 集成测试场景
- 手动验证清单

---

## 7. 数据库变更

涉及新增数据库表、新增字段或初始化数据时，SQL 脚本独立存放在版本目录下：

- DDL 脚本：`docs/prd/<version>/sql/DDL.sql`
- DML 脚本：`docs/prd/<version>/sql/DML_init.sql`

---

## 8. 部署考虑

- 环境配置变更
- 数据迁移（如有）
- 回滚方案
